{
  "name": "les-gates-tests",
  "private": true,
  "scripts": {
    "reglas": "firebase emulators:exec --only firestore --project demo-gates \"node reglas.mjs\"",
    "recorrido": "firebase emulators:exec --only firestore,auth --project michis-en-casa \"node recorrido-multiple.cjs\"",
    "test": "npm run reglas && npm run recorrido && npm run turnos",
    "turnos": "firebase emulators:exec --only firestore,auth --project michis-en-casa \"node turnos.cjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.0",
    "firebase": "^10.13.0",
    "firebase-tools": "^13.0.0",
    "playwright": "^1.56.0"
  }
}
